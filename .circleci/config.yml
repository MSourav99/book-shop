version: 2.1
jobs:
  runjob:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run: |
          JOB_ID=$(cat job_id.txt)
          echo "JOB_ID=$JOB_ID"
          echo "START=$(date +%s)"
          if [ "$JOB_ID" -le 29 ]; then
            for n in $(seq 1 20); do echo "tick $n"; sleep 30; done
          else
            sleep 60
          fi
          echo "END=$(date +%s)"
workflows:
  build:
    jobs:
      - runjob
